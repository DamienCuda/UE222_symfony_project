{% extends 'base.html.twig' %}

{% block title %}Login{% endblock %}

{% block header %}{% endblock %}


{% block body %}
    {# Modal d'identifiants #}
    {{ include('security/_modal_login.html.twig') }}

    {# Section titres #}
    <section id="login_header" class="text-center mb-5 mt-5">
        <h1>Bienvenue sur le blog du groupe 5</h1>
        <p>Entrez votre mot nom d'utilisateur et votre mot de passe pour accéder aux articles</p>
    </section>

    {# Formulaire de connexion #}
    {{ include('security/_form_login.html.twig') }}
    

        {# Modal toggle #}
    <div class="mt-3 d-flex justify-content-center">
        <a class="lead" href="{{path('app_register')}}"><p class="lead pr-3">Inscrivez-vous<p></a>
        <a href="#"><p id="forgotten_password_btn" class="lead" data-bs-toggle="modal" data-bs-target="#login_modal">Besoin d'identifiants ?</p></a>
    </div>
{% endblock %}
