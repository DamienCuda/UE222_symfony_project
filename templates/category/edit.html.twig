{% extends 'base.html.twig' %}

{% block title %}Modifier Categorie{% endblock %}

{% block body %}
    {{parent()}}
    <main class="container">
        <h1 class="my-4"><i class='bx bx-edit-alt' ></i> Modifier une catégorie</h1>
        {{ include('category/_form.html.twig', {'button_label': 'Modifier'}) }}
        <div class="delete_edit">
        <a href="{{ path('category_index') }}"><button class="btn btn-primary d-flex justify-content-between align-items-center mt-2"><i class='bx bx-left-arrow-alt'></i><span>Retour</span></button></a>
        {{ include('category/_delete_form.html.twig') }}
        </div>
    </main>
{% endblock %}
