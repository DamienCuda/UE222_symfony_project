<div class="card">
  <div class="card-header d-flex align-items-center"><i class='bx bx-folder-plus'></i><span>Ajouter une catégorie</span></div>
  <div class="card-body">
    {{ form_start(form) }}
       {{ form_label(form.name, 'Nom de la catégorie') }}
        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
        <button class="btn btn-primary mt-2">{{ button_label|default('Ajouter') }}</button>
    {{ form_end(form) }}
  </div>
</div>