<div class="card">
  <div class="card-header d-flex align-items-center"><i class='bx bx-file' ></i>Article</span></div>
  <div class="card-body">
    {{ form_start(form) }}
        {{ form_label(form.title, 'Titre') }}
        {{ form_widget(form.title, {'attr': {'class': 'form-control mt-1 mb-2'}}) }}
        {{ form_label(form.content, 'Contenue de l\'article') }}
        {{ form_widget(form.content, {'attr': {'class': 'form-control mt-1 mb-2'}}) }}
        {{ form_label(form.category, 'Catégorie') }}
        {{ form_widget(form.category, {'attr': {'class': 'form-select mt-1 mb-2'}}) }}
        <button class="btn btn-primary mt-2">{{ button_label|default('Ajouter') }}</button>
    {{ form_end(form) }}
  </div>
</div>