{% extends 'base.html.twig' %}

{% block title %}Modifier un Article{% endblock %}

{% block body %}
    {{parent()}}
    <main class="container">
        <h1>Modifier un Article</h1>

        {{ include('article/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        <div class="delete_edit">
        <a href="{{ path('article_index') }}" class="btn btn-primary d-flex justify-content-between align-items-center"><i class='bx bx-left-arrow-alt'></i><span>Retour</span></a>
        {{ include('article/_delete_form.html.twig') }}
        </div>
    </main>
{% endblock %}
